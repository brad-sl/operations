# Crypto Trading Bot - Phase 6 runtime dependencies
# Install with: pip install -r requirements.txt

aiohttp>=3.9
python-dotenv>=1.0
requests>=2.31
pandas>=2.0
numpy>=1.26
# For backtests and analysis (optional but recommended)
matplotlib>=3.8
# For sentiment / external (if using Apify or similar)
# apify-client
# For scheduling / monitoring
# schedule

# Note: Coinbase Advanced Trade uses their SDK or direct REST + JWT.
# Add specific if using coinbase-advanced-trade-python or similar.
