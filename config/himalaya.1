.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.TH himalaya 1  "himalaya 2.0.0" 
.SH NAME
himalaya \- CLI to manage emails
.SH SYNOPSIS
\fBhimalaya\fR [\fB\-c\fR|\fB\-\-config\fR] [\fB\-a\fR|\fB\-\-account\fR] [\fB\-b\fR|\fB\-\-backend\fR] [\fB\-\-json\fR] [\fB\-\-log\-level\fR] [\fB\-\-log\-file\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fB\-V\fR|\fB\-\-version\fR] [\fIsubcommands\fR]
.SH DESCRIPTION
CLI to manage emails
.SH OPTIONS
.TP
\fB\-c\fR, \fB\-\-config\fR \fI<PATH>\fR
Override the default configuration file path.

The given paths are shell\-expanded then canonicalized (if applicable). If the first path does not point to a valid file, the wizard will propose to assist you in the creation of the configuration file. Other paths are merged with the first one, which allows you to separate your public config from your private(s) one(s). Multiple paths can also be provided by delimiting them with `:` (like `$PATH` in a POSIX shell).
.TP
\fB\-a\fR, \fB\-\-account\fR \fI<NAME>\fR
Override the default account.

An account name corresponds to an entry in the table at the root level of your TOML configuration file.
.TP
\fB\-b\fR, \fB\-\-backend\fR \fI<BACKEND>\fR [default: auto]
Force a specific backend for cross\-protocol commands.

Only consumed by the shared commands (`mailboxes`, `envelopes`, `flags`, `messages`); the protocol\-specific subcommands ignore it and always use their own backend. With `auto` (the default) the shared command picks the first configured backend it supports; with an explicit value it uses only that backend, and bails if the account has no matching config block or the operation has no implementation for it (e.g. `\-\-backend smtp mailboxes list`).
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
auto
.IP \(bu 2
imap
.IP \(bu 2
jmap
.IP \(bu 2
gmail
.IP \(bu 2
msgraph
.IP \(bu 2
maildir
.IP \(bu 2
m2dir
.IP \(bu 2
smtp
.RE
.TP
\fB\-\-json\fR
Enable JSON output.

When set, command output (data or errors) is displayed as JSON string.
.TP
\fB\-\-log\-level\fR \fI<LEVEL>\fR
Filter log output by level.

When omitted, the `RUST_LOG` environment variable is consulted (it supports per\-target filters — see the `env_logger` documentation). When present, this flag overrides `RUST_LOG` entirely.
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
off: No logging
.IP \(bu 2
error: Errors only
.IP \(bu 2
warn: Warnings and errors
.IP \(bu 2
info: Informational messages and above
.IP \(bu 2
debug: Debug messages and above
.IP \(bu 2
trace: Trace messages and above (most verbose)
.RE
.TP
\fB\-\-log\-file\fR \fI<PATH>\fR
Append log output to the given file instead of stderr.

Useful when interactive prompts (which always write to stderr) would otherwise be intermixed with log lines. The file is opened in append mode and created if it does not exist.
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
\fB\-V\fR, \fB\-\-version\fR
Print version
.SH SUBCOMMANDS
.TP
himalaya\-mailbox(1)
Manage mailboxes using the shared API
.TP
himalaya\-envelope(1)
Manage envelopes using the shared API
.TP
himalaya\-flag(1)
Manage flags using the shared API
.TP
himalaya\-message(1)
Manage messages using the shared API
.TP
himalaya\-attachment(1)
Manage attachments using the shared API
.TP
himalaya\-imap(1)
IMAP\-specific API
.TP
himalaya\-jmap(1)
JMAP\-specific API
.TP
himalaya\-gmail(1)
Gmail\-specific API
.TP
himalaya\-msgraph(1)
Microsoft Graph CLI
.TP
himalaya\-m2dir(1)
M2dir\-specific API
.TP
himalaya\-smtp(1)
SMTP\-specific API
.TP
himalaya\-account(1)
Manage accounts defined in the TOML configuration file
.TP
himalaya\-completion(1)
Generate completion script for the give shell(s) to the given directory
.TP
himalaya\-manual(1)
Generate manual pages to the given directory
.TP
himalaya\-json\-schema(1)
Generate JSON Schemas of every command\*(Aqs JSON output to the given directory
.TP
himalaya\-help(1)
Print this message or the help of the given subcommand(s)
.SH VERSION
vv2.0.0 +smtp +gmail +imap +m2dir +msgraph +jmap +rustls\-ring
build: linux musl x86_64
git: nix\-flake\-20260726001142, rev b1f6dece32c3afc97d44adeb148bf87e89fde140
.SH AUTHORS
Clément DOUIN (soywod) <pimalaya.org@posteo.net>
