FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir flask gunicorn

COPY health_gateway.py /app/app.py

EXPOSE 8000

# Use gunicorn for prod-like
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "--workers", "2", "app:app"]
