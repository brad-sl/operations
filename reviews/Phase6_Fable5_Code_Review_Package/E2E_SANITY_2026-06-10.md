# Phase 6 E2E Sanity Sign-off after Batch 3/4 Non-Criticals + Live Items (#1 & #2)

**Date:** 2026-06-10
**Scotty sign-off** after fixes for the two requested groups and full scenario sanity checks in shadow/paper.

## Group #1 (G3/G4 leaks + related)
- P6-151: LPM + exchange_client.get_enriched_positions always return structured sentinel. Isolation test + E2E Fresh Start pass. No bare {} leak.
- P6-142 + G4 funding: rebalance_plan now robust to "value_usd", "usd_value", bare float. Runner uses projected targets for enforce. Isolation + E2E rebalance pass.
- Reserve config + projection already hardened earlier (config key, runner call with targets, harness telemetry).

## Group #2 (Live safety P6-144/145/146)
- P6-144: private_key newline normalization in both live init paths. Test passes.
- P6-145: place_stop_limit_sell now uses _quantize helpers + no "reduce_only" (the hard blocker shown in real logs). 
- P6-146: ADA-USD metadata (0.0001 price_increment) added to get_product_metadata.

All isolation tests green.

## E2E Scenarios Exercised (shadow + paper harness)
1. Fresh Start (verified zero) - sentinel + G2 guard + no phantom positions.
2. Daily Rebalance with holdings (mixed key shapes + reserve projection).
3. Stop attachment path (DOGE + ADA, quantized, no invalid fields).
4. Error/injected failure paths (harness tick-2 + LPM sentinel paths).
5. Sentiment aging + weight adjustment in harness (real v3 cache).
6. Reserve telemetry on every cycle (G4).

Paper harness (multiple 4-8 tick runs) + dedicated E2E scripts all clean except the intentionally injected error (as designed for G2 test).

## Final Gate Notes for Paper → Live
- Paper pre-start blockers (141 + G4/143/148) closed.
- Major live blockers (144/145/146) closed.
- G3 sentinel leak closed.
- Evidence attached in scripts/test_fable5_* and this artifact.
- Remaining for extended paper: full 48h run with forced failures, tighter funding param in rebalance_plan (comment present), real /products fetch (table is sufficient for now).

**Scotty sign-off (2026-06-10):** All #1 and #2 items addressed with Code Isolation Testing + real-data shadow verification. E2E for the key trading scenarios (Fresh Start, Rebalance, Stops, Error paths, Reserve, Sentiment) exercised. No new criticals surfaced. 

The system is at the state where extended paper observation (or full end-to-end per-scenario suite in long run) is the appropriate next step before any live capital exposure.

User to decide: launch long paper harness now, or one more targeted wave (e.g. full funding param + more forced tests), then live re-gate.

Evidence-based, no hype. Ready when you are.