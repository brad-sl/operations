# crypto-trading-bot — Hermes project rules

## Preflight (non-trivial work) — mandatory load

When the ask is a **non-trivial plan/design**, risk/product change, promote path, or multi-file feature — **before** coding or BoN:

1. Call `skill_view(name='brad-preflight')`
2. Chain: **grill-me → plan (`/plan` or writing-plans) → BoN only if ≥2 options → wait for Brad go**
3. Default **must not touch** live book / prod knobs / auto-promote until explicit **go**
4. Mid-run: prefer `/btw` and `/steer` over restarting the session

**Triggers:** “preflight”, “grill me”, “how should we do X”, schema/state migration, exit/TP/SL/seats/sizing/soft_down/park, MASTER `preflight: true`

**Skip full preflight:** one-line fix, locked “do GAP-…”, single-RCA firefight, cron tick, “done messing” Q&A, already-said **Go** (then execute only)

## Decision quality (BoN / LLM verifier) — mandatory load

When **any** of the following is true, **before** giving a single recommended next task or approach:

1. Call `skill_view(name='best-of-n-verify')`
2. Run the skill procedure (freeze criteria → N=3 angled gens → score → margin gate)
3. Do **not** invent a lone “winner” if margin &lt; 1.5 — escalate + optional ground-truth

If the fork is still a vague design tree, run **brad-preflight** (grill + plan) first so BoN scores crisp options.

**Triggers**

- Staff / priority: “staff next”, “which gap”, “what should we work on”, bang-for-buck among scale gaps
- Explicit fork: “A vs B”, “pick between”, ≥2 OPEN/QUEUED competing MASTER items
- Approach fork: ≥2 plausible code/design paths with real cost of being wrong
- Promote readiness: competing analyst packets / go-no-go with disagreement
- User says BoN / verifier / best-of-n / MASTER `bon_verify: true`

**Skip BoN**

- User already locked one option (“do GAP-05”, “execute C”)
- Single-path bugfix or firefight with one clear RCA
- Live order / capital / TP / SL / USDC / mid-cycle changes
- Routine ops triage / cron noise

**After pick:** execute winner; stage runner-up as backup when indeterminate. **Go after BoN = execute only** — no re-BoN.

Pilot reference: `reports/bon_verify_pilot/`  
Rank helper: `~/.hermes/skills/software-development/best-of-n-verify/scripts/score_candidates.py`

## Other standing rules

- MASTER_TASK_TRACKING.md is durable SSOT; Kanban is dispatcher.
- No fake prices/fills; real data only on prod paths.
- Isolation test + show output before closing output-producing work.
- Leave live book as-is unless Brad go; OPT→shadow→go; dual_agree≠promote.
