# Trial review inbox

Uncertain or pending Brad decisions land here as `REVIEW_*.md` / `DECIDED_*.md`.

Close with:
```bash
cd /home/brad/projects/crypto-trading-bot
python3 phase6/research/trial_cycle.py decide <TRIAL_ID> <enum> --note "..."
```

