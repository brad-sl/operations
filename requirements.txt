# Crypto Trading Bot - Phase 6 runtime dependencies
# Install with: pip install -r requirements.txt (from project .venv)

aiohttp>=3.9
python-dotenv>=1.0
requests>=2.31
pandas>=2.0
numpy>=1.26
# For backtests and analysis (optional but recommended)
matplotlib>=3.8
# For sentiment / external APIs (used by fetch_x_sentiment.py and fetch_reddit_sentiment.py)
apify-client>=1.0
textblob>=0.17

# Note: Coinbase Advanced Trade uses their SDK or direct REST + JWT.
# Add specific if using coinbase-advanced-trade-python or similar.

# Postgres + migrations (T0-01 multi-tenant registry)
sqlalchemy[asyncio]>=2.0
alembic>=1.13
asyncpg>=0.29
# cryptography already in base for JWT/encryption (used by oauth_tokens encrypt)

